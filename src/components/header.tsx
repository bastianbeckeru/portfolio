import Link from 'next/link';

export default function Header() {
  return (
    <header className='h-16 w-full fixed top-0 z-50 backdrop-blur-md [mask-image:linear-gradient(to_bottom,black_0%,transparent_100%)]'></header>
  );
}
